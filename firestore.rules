rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // In this capacity building & learning portal prototype with demo roles & Auth
      allow read, write: if true;
    }
  }
}
